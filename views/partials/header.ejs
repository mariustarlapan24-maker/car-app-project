<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/png" href="/images/favicon.png">
</head>
<body>
    <header class="header">
        <h1 class="app-title">Car-App</h1>
        <% if (isGuest) { %>
            <div class="guest-notice">
                Navighezi ca invitat. Unele funcții sunt limitate.
                <form action="/login" method="GET" style="display:inline;">
                    <button type="submit" class="btn-guest-login">Login / Înregistrare</button>
                </form>
            </div>
        <% } %>
    </header>
    <main class="main-container">
