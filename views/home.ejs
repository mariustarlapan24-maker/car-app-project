<%- include('partials/header', { title: 'Car-App - Acasă' }) %>

<div class="container mt-4">
    <% if (!isLoggedIn) { %>
        <div class="alert alert-info text-center">
            Răsfoiești ca invitat. <a href="/login" class="alert-link">Login</a> pentru acces complet.
        </div>
    <% } %>

    <div class="mb-4">
        <div class="input-group">
            <span class="input-group-text">
                <img src="/img/drapel-md.png" alt="MD Flag" style="height:20px;"> MD
            </span>
            <input type="text" id="plateSearchInput" class="form-control" placeholder="Caută după număr ...">
            <button class="btn btn-outline-secondary" id="clearSearch" style="display:none;">❌</button>
        </div>
        <div id="searchResults" class="list-group mt-2"></div>
    </div>

    <section class="general-listings">
        <!-- Aici poți pune listările generale de mașini -->
    </section>
</div>

<script src="/js/home.js"></script>
<%- include('partials/footer') %>
